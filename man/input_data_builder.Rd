% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-builder.R
\name{input_data_builder}
\alias{input_data_builder}
\alias{with_kpi}
\alias{with_population}
\alias{with_controls}
\alias{with_revenue_per_kpi}
\alias{with_media}
\alias{with_reach}
\alias{with_organic_media}
\alias{with_non_media_treatments}
\alias{build_input_data}
\title{Build Meridian input data with a fluent builder}
\usage{
input_data_builder(
  kpi_type = c("non_revenue", "revenue"),
  geo = "geo",
  time = "time",
  media_time = time,
  population = "population",
  kpi = "kpi",
  revenue_per_kpi = "revenue_per_kpi"
)

with_kpi(builder, data, kpi_col = NULL, time_col = NULL, geo_col = NULL)

with_population(builder, data, population_col = NULL, geo_col = NULL)

with_controls(builder, data, control_cols, time_col = NULL, geo_col = NULL)

with_revenue_per_kpi(
  builder,
  data,
  revenue_per_kpi_col = NULL,
  time_col = NULL,
  geo_col = NULL
)

with_media(
  builder,
  data,
  media_cols,
  media_spend_cols,
  media_channels,
  time_col = NULL,
  geo_col = NULL
)

with_reach(
  builder,
  data,
  reach_cols,
  frequency_cols,
  rf_spend_cols,
  rf_channels,
  time_col = NULL,
  geo_col = NULL
)

with_organic_media(
  builder,
  data,
  organic_media_cols,
  organic_media_channels = NULL,
  media_time_col = NULL,
  geo_col = NULL
)

with_non_media_treatments(
  builder,
  data,
  non_media_treatment_cols,
  time_col = NULL,
  geo_col = NULL
)

build_input_data(builder)
}
\arguments{
\item{kpi_type}{Either \code{"non_revenue"} or \code{"revenue"}.}

\item{geo, time, media_time, population, kpi, revenue_per_kpi}{Default column names
the builder uses when a specific \verb{*_col} argument is not supplied to a verb.}

\item{builder}{A \code{meridian_input_builder} from \code{\link[=input_data_builder]{input_data_builder()}}.}

\item{data}{A data frame containing the referenced columns.}

\item{kpi_col, time_col, geo_col, population_col, revenue_per_kpi_col, media_time_col}{Optional column-name overrides; default to the builder's defaults.}

\item{control_cols}{Character vector of control-variable column names.}

\item{media_cols}{Character vector of media \emph{execution} columns (e.g.
impressions or clicks).}

\item{media_spend_cols}{Character vector of media \emph{spend} columns, aligned
with \code{media_cols}.}

\item{media_channels}{Character vector of channel names for the media.}

\item{reach_cols, frequency_cols, rf_spend_cols}{Character vectors of reach,
average-frequency, and spend columns for reach-and-frequency channels.}

\item{rf_channels}{Character vector of channel names for the R&F channels.}

\item{organic_media_cols}{Character vector of organic-media execution columns.}

\item{organic_media_channels}{Optional channel names for organic media.}

\item{non_media_treatment_cols}{Character vector of non-media treatment
columns (e.g. promotions, price changes).}
}
\value{
\code{input_data_builder()} and every \verb{with_*()} verb return a
\code{meridian_input_builder}. \code{\link[=build_input_data]{build_input_data()}} returns a
\code{meridian_input_data} object.
}
\description{
\code{input_data_builder()} creates a Meridian \code{DataFrameInputDataBuilder}, and the
\verb{with_*()} verbs add each data component from one or more R data frames.
Finish the chain with \code{\link[=build_input_data]{build_input_data()}}. This is the flexible, pipe-
friendly path; for the common case, \code{\link[=as_meridian_input]{as_meridian_input()}} wraps the whole
chain in a single call.
}
\details{
Each verb returns the (modified) builder, so calls compose with the native
pipe:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{input_data_builder("non_revenue") |>
  with_kpi(df) |>
  with_population(df) |>
  with_controls(df, control_cols = c("gqv", "competitor_sales")) |>
  with_media(df,
    media_cols       = c("tv_impressions", "search_clicks"),
    media_spend_cols = c("tv_spend", "search_spend"),
    media_channels   = c("TV", "Search")) |>
  build_input_data()
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=as_meridian_input]{as_meridian_input()}} for the one-call interface.
}
