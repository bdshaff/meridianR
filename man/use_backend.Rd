% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{use_backend}
\alias{use_backend}
\title{Select the Meridian compute backend}
\usage{
use_backend(backend = c("tensorflow", "jax"))
}
\arguments{
\item{backend}{\code{"tensorflow"} or \code{"jax"}.}
}
\value{
The selected backend, invisibly.
}
\description{
Meridian can run on a TensorFlow (default) or an experimental JAX backend.
The backend is read from the \code{MERIDIAN_BACKEND} environment variable when the
Meridian Python module is first imported, so for a reliable switch call
\code{use_backend()} \emph{before} any other meridianR function in a session. If
Meridian is already loaded, \code{use_backend()} additionally attempts a runtime
switch, which may not take full effect until the session is restarted.
}
\examples{
\dontrun{
use_backend("jax")
}
}
