% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-input.R
\name{as_meridian_input}
\alias{as_meridian_input}
\title{Assemble Meridian input data from a data frame}
\usage{
as_meridian_input(
  data,
  kpi,
  time = "time",
  geo = "geo",
  population = "population",
  media_spend = NULL,
  media = NULL,
  media_channels = NULL,
  controls = NULL,
  revenue_per_kpi = NULL,
  reach = NULL,
  frequency = NULL,
  rf_spend = NULL,
  rf_channels = NULL,
  organic_media = NULL,
  non_media_treatments = NULL,
  kpi_type = c("non_revenue", "revenue"),
  national = FALSE
)
}
\arguments{
\item{data}{A long-format data frame.}

\item{kpi}{Name of the KPI column.}

\item{time}{Name of the time column (dates are coerced to ISO-8601 strings).}

\item{geo}{Name of the geo column. Ignored (and synthesized) when
\code{national = TRUE}.}

\item{population}{Name of the per-geo population column. Synthesized as a
constant when \code{national = TRUE} and absent.}

\item{media_spend}{Character vector of media spend columns.}

\item{media}{Character vector of media execution columns (e.g. impressions),
aligned with \code{media_spend}. Defaults to \code{media_spend}.}

\item{media_channels}{Character vector of channel labels. Defaults to
\code{media_spend}.}

\item{controls}{Character vector of control-variable columns.}

\item{revenue_per_kpi}{Name of the revenue-per-KPI column, if any.}

\item{reach, frequency, rf_spend}{Character vectors of reach, average-frequency,
and spend columns for reach-and-frequency channels.}

\item{rf_channels}{Channel labels for R&F channels. Defaults to \code{reach}.}

\item{organic_media}{Character vector of organic-media columns.}

\item{non_media_treatments}{Character vector of non-media treatment columns.}

\item{kpi_type}{\code{"non_revenue"} (default) or \code{"revenue"}.}

\item{national}{If \code{TRUE}, treat the data as national (single geo): a geo
column and constant population are synthesized when absent.}
}
\value{
A \code{meridian_input_data} object wrapping a Meridian \code{InputData}.
}
\description{
\code{as_meridian_input()} is the primary, one-call way to turn a long-format R
data frame into a Meridian \code{InputData} object. It drives Meridian's
\code{DataFrameInputDataBuilder} (see \code{\link[=input_data_builder]{input_data_builder()}}) under the hood,
adding only the components you supply.
}
\details{
The data must be in long format: one row per geo-by-time (or per time for a
national model), with columns for the KPI, population, media spend, and any
controls. Media, reach/frequency, organic media, and non-media treatments are
optional.

If you supply \code{media_spend} but not \code{media}, spend is also used as the media
execution metric (a spend-based model). Channel labels default to the spend
column names; pass \code{media_channels} for nicer labels.
}
\examples{
\dontrun{
input <- as_meridian_input(
  sales,
  kpi         = "conversions",
  time        = "week",
  geo         = "region",
  population  = "population",
  media_spend = c("tv_spend", "search_spend"),
  media       = c("tv_impressions", "search_clicks"),
  media_channels = c("TV", "Search"),
  controls    = "price_index"
)
input
}
}
\seealso{
\code{\link[=input_data_builder]{input_data_builder()}} for the step-by-step builder, and
\code{\link[=meridian_model]{meridian_model()}} to fit a model on the result.
}
