% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{sample_prior}
\alias{sample_prior}
\alias{sample_posterior}
\title{Draw from a Meridian model's prior and posterior}
\usage{
sample_prior(model, n_draws = 500, seed = NULL)

sample_posterior(
  model,
  n_chains = 7,
  n_adapt = 500,
  n_burnin = 500,
  n_keep = 1000,
  seed = NULL,
  ...
)
}
\arguments{
\item{model}{A \code{meridian_model} from \code{\link[=meridian_model]{meridian_model()}}.}

\item{n_draws}{Number of prior draws.}

\item{seed}{Optional integer (or vector) random seed.}

\item{n_chains}{Number of MCMC chains (an integer, or a vector of integers to
split sampling into several calls to reduce peak memory).}

\item{n_adapt, n_burnin, n_keep}{Number of adaptation, burn-in, and kept draws
per chain.}

\item{...}{Further arguments forwarded to Meridian's \code{sample_posterior()}
(e.g. \code{max_tree_depth}, \code{parallel_iterations}).}
}
\value{
The (modified) \code{meridian_model}, invisibly.
}
\description{
\code{sample_prior()} and \code{sample_posterior()} run Meridian's prior and MCMC
posterior sampling. Both \strong{modify the model in place} (drawn samples are
merged into the model's ArviZ \code{inference_data}) and return the same model
object invisibly, so they compose with the native pipe. \code{\link[=fit]{fit()}} is a
convenience that runs both.
}
\seealso{
\code{\link[=meridian_model]{meridian_model()}}, \code{\link[=fit]{fit()}}
}
