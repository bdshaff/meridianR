% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{prior_distribution}
\alias{prior_distribution}
\title{Prior distributions for a Meridian model}
\usage{
prior_distribution(...)
}
\arguments{
\item{...}{Named priors, e.g. \code{roi_m = prior_lognormal(0.2, 0.9)}.}
}
\value{
A \code{meridian_prior} object.
}
\description{
\code{prior_distribution()} builds Meridian's \code{PriorDistribution}, the container of
priors used by a \code{\link[=model_spec]{model_spec()}}. Supply named priors built with the
\verb{prior_*()} distribution constructors; anything you omit keeps Meridian's
default. The most commonly customized priors are the ROI priors \code{roi_m}
(media) and \code{roi_rf} (reach & frequency).
}
\details{
Accepted names are the fields of Meridian's \code{PriorDistribution}, including:
\code{roi_m}, \code{roi_rf}, \code{mroi_m}, \code{mroi_rf}, \code{contribution_m}, \code{contribution_rf},
\code{beta_m}, \code{beta_rf}, \code{alpha_m} (adstock), \code{ec_m}, \code{slope_m} (Hill saturation),
\code{sigma}, \code{tau_g_excl_baseline}, and \code{knot_values}.
}
\examples{
\dontrun{
prior_distribution(
  roi_m  = prior_lognormal(0.2, 0.9),
  roi_rf = prior_lognormal(0.2, 0.9)
)
}
}
\seealso{
\code{\link[=prior_lognormal]{prior_lognormal()}} and friends for the distribution constructors;
\code{\link[=model_spec]{model_spec()}} to attach the priors to a model configuration.
}
