% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-spec.R
\name{model_spec}
\alias{model_spec}
\title{Configure a Meridian model}
\usage{
model_spec(
  prior = NULL,
  media_effects_dist = NULL,
  hill_before_adstock = NULL,
  max_lag = NULL,
  unique_sigma_for_each_geo = NULL,
  paid_media_prior_type = NULL,
  rf_prior_type = NULL,
  knots = NULL,
  baseline_geo = NULL,
  holdout_id = NULL,
  adstock_decay_spec = NULL,
  saturation_spec = NULL,
  ...
)
}
\arguments{
\item{prior}{A \code{meridian_prior} from \code{\link[=prior_distribution]{prior_distribution()}}.}

\item{media_effects_dist}{\code{"log_normal"} (Meridian default) or \code{"normal"}.}

\item{hill_before_adstock}{Apply Hill saturation before adstock? Defaults to
\code{FALSE} in Meridian.}

\item{max_lag}{Maximum adstock lag, in time periods (integer; Meridian
default 8).}

\item{unique_sigma_for_each_geo}{Estimate a separate residual standard
deviation per geo?}

\item{paid_media_prior_type, rf_prior_type}{How the paid-media and
reach-and-frequency priors are parameterized, e.g. \code{"roi"}, \code{"mroi"},
\code{"contribution"}, or \code{"coefficient"}.}

\item{knots}{Number of spline knots (a single integer) or a vector of knot
positions, controlling the time-varying baseline.}

\item{baseline_geo}{Baseline geo, given as a geo name or an integer index.}

\item{holdout_id}{Optional array/boolean matrix marking held-out
observations for out-of-sample evaluation.}

\item{adstock_decay_spec}{Adstock decay function: \code{"geometric"} (default) or
\code{"binomial"}, or a named list giving a function per channel.}

\item{saturation_spec}{Saturation function: \code{"hill"} (default), or a named
list per channel.}

\item{...}{Further arguments forwarded to \code{ModelSpec}, such as
\code{media_prior_type}, \code{roi_calibration_period}, \code{organic_media_prior_type},
\code{non_media_treatments_prior_type}, or \code{enable_aks}.}
}
\value{
A \code{meridian_model_spec} object.
}
\description{
\code{model_spec()} builds Meridian's \code{ModelSpec}, which controls the adstock/lag
structure, saturation, media-effect distribution, prior types, spline knots,
and calibration. Any argument left \code{NULL} uses Meridian's own default. Attach
custom priors by passing a \code{\link[=prior_distribution]{prior_distribution()}} to \code{prior}.
}
\examples{
\dontrun{
spec <- model_spec(
  prior              = prior_distribution(roi_m = prior_lognormal(0.2, 0.9)),
  max_lag            = 8,
  media_effects_dist = "log_normal"
)
}
}
\seealso{
\code{\link[=prior_distribution]{prior_distribution()}}, \code{\link[=meridian_model]{meridian_model()}}
}
